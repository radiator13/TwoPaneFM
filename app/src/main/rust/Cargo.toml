[package]
name = "fileops"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "fileops"

[dependencies]
jni = "0.21"
libc = "0.2"
filetime = "0.2"

[profile.release]
opt-level = 2
lto = true
strip = true
